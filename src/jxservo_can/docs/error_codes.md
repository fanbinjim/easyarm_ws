# 巨蟹智能 V2.0 错误码与 SDO 读取摘要

资料来源：FC0001 V2.0报错代码&SDO读取，Rev. 1.0，2026-05-11。

## 读取对象

| 对象 | 名称 | 长度 | 返回内容 | 推荐用途 |
| --- | --- | --- | --- | --- |
| `0x603F:00` | `errorCode` | 2 字节 | 厂家兼容性报错 | 兼容旧版上位机或粗略分类 |
| `0x4500:00` | `currentErrorID` | 2 字节 | 新版诊断 ID 的低 16 位 `[故障域][子项]` | V2.0 及以后精确定位 |

两个对象都返回最近一次告警或故障记录。当前没有可读取告警或故障时返回 `0x0000`。

`0x603F` 会做兼容映射，一个兼容码可能对应多个新版故障项。新项目应优先读取 `0x4500`，只把 `0x603F` 作为兼容显示字段。

## SDO 帧格式

以 `Dev_ID = 1` 为例：

- 主站发送 COB-ID：`0x601`。
- 设备响应 COB-ID：`0x581`。
- SDO 读请求命令字：`0x40`。
- 2 字节读响应命令字：`0x4B`。
- 多字节返回数据为小端，响应 `LL HH` 还原为 `0xHHLL`。

读取厂家兼容性报错 `0x603F`：

```text
Tx 0x601 DLC 8: 40 3F 60 00 00 00 00 00
Rx 0x581 DLC 8: 4B 3F 60 00 LL HH 00 00
兼容错误码 = 0xHHLL
```

读取新版错误 ID 低 16 位 `0x4500`：

```text
Tx 0x601 DLC 8: 40 00 45 00 00 00 00 00
Rx 0x581 DLC 8: 4B 00 45 00 LL HH 00 00
新版错误 ID 低 16 位 = 0xHHLL
高字节 HH = 故障域
低字节 LL = 子项
```

示例：

```text
4B 00 45 00 05 02 00 00
```

表示 `0x4500 = 0x0205`，故障域 `0x02`，子项 `0x05`，R68LITE 示例中对应驱动过温故障。

## V2.0 错误 ID 结构

设备内部使用 32 位诊断 ID：

```text
[严重度][预留ID][故障域][子项]
   1B      1B      1B    1B
```

`0x4500` 只返回后 2 字节：

```text
[故障域][子项]
   1B    1B
```

`0x4500` 不直接返回严重度字段。若要区分告警、故障或跳闸等级，需要结合设备状态、日志或对应产品诊断说明。

## 故障域定义

| 故障域 | 名称 | 说明 |
| --- | --- | --- |
| `0x00` | 系统 | 参数、配置、内部状态等系统类问题 |
| `0x01` | 电源 | 母线电压、电源输入和母线硬件保护 |
| `0x02` | 驱动 | 功率驱动、驱动温度、相电流和驱动侧保护 |
| `0x03` | 电机 | 电机温度、电机观测和电机侧状态 |
| `0x04` | 传感器 | 编码器、电流采样、母线采样等传感器状态 |
| `0x05` | 控制 | 位置、速度、轨迹、设定点和运动控制状态 |
| `0x06` | 通信 | 心跳、指令流、CAN 通信等链路状态 |
| `0x07` | 安全 | 软件限制、限位、状态机安全保护 |
| `0x08` | 应用 | 标定、应用流程和应用层安全检查 |

## R68LITE 示例故障表

该表是 V2.0 通用编码规则示例，不代表所有产品启用相同条目。实际使用应以对应产品发布资料为准。

| `0x4500` | 完整 32 位 ID | 严重度 | 故障域 | 说明 |
| --- | --- | --- | --- | --- |
| `0x0100` | `0x02000100` | 故障 | 电源 | 母线电压过高 |
| `0x0101` | `0x02000101` | 故障 | 电源 | 母线电压过低 |
| `0x0102` | `0x03000102` | 跳闸 | 电源 | 母线硬件过压跳闸 |
| `0x0103` | `0x03000103` | 跳闸 | 电源 | 母线硬件过流跳闸 |
| `0x0200` | `0x02000200` | 故障 | 驱动 | 软件过流 |
| `0x0201` | `0x03000201` | 跳闸 | 驱动 | U 相硬件过流跳闸 |
| `0x0203` | `0x03000203` | 跳闸 | 驱动 | V 相硬件过流跳闸 |
| `0x0204` | `0x01000204` | 告警 | 驱动 | 驱动过温告警 |
| `0x0205` | `0x02000205` | 故障 | 驱动 | 驱动过温故障 |
| `0x0206` | `0x03000206` | 跳闸 | 驱动 | U 相软件过流 |
| `0x0207` | `0x03000207` | 跳闸 | 驱动 | V 相软件过流 |
| `0x0302` | `0x01000302` | 告警 | 电机 | 电机过温告警 |
| `0x0303` | `0x02000303` | 故障 | 电机 | 电机过温故障 |
| `0x0400` | `0x03000400` | 跳闸 | 传感器 | 编码器超时未更新 |
| `0x0401` | `0x01000401` | 告警 | 传感器 | 编码器读取失败 |
| `0x0406` | `0x01000406` | 告警 | 传感器 | 编码器状态告警 |
| `0x0407` | `0x02000407` | 故障 | 传感器 | 编码器状态错误 |
| `0x0408` | `0x02000408` | 故障 | 传感器 | 电流偏置校准失败 |
| `0x0500` | `0x02000500` | 故障 | 控制 | 位置跟踪误差过大 |
| `0x0507` | `0x02000507` | 故障 | 控制 | 速度跟踪误差过大 |
| `0x0508` | `0x02000508` | 故障 | 控制 | 软件堵转 |
| `0x0509` | `0x01000509` | 告警 | 控制 | 速度指令超出上限 |
| `0x050A` | `0x0200050A` | 故障 | 控制 | 飞车或超速保护 |
| `0x0600` | `0x02000600` | 故障 | 通信 | 通信心跳丢失 |
| `0x0603` | `0x01000603` | 告警 | 通信 | CAN 通信 BusOff |
| `0x0705` | `0x02000705` | 故障 | 安全 | 正向光电限位超出 |
| `0x0706` | `0x02000706` | 故障 | 安全 | 负向光电限位超出 |
| `0x0707` | `0x02000707` | 故障 | 安全 | 正向软件限位超出 |
| `0x0708` | `0x02000708` | 故障 | 安全 | 负向软件限位超出 |

## 兼容码示例

| 新版 `0x4500` 示例 | 新版含义 | `0x603F` 兼容分类示例 |
| --- | --- | --- |
| `0x0100` | 母线电压过高 | `0x0001` 母线过压 |
| `0x0101` | 母线电压过低 | `0x0002` 母线欠压 |
| `0x0205` / `0x0303` | 驱动或电机过温故障 | `0x0004` 过温 |
| `0x0508` | 软件堵转 | `0x0008` 堵转 |
| `0x0507` | 速度跟踪误差过大 | `0x0020` 速度误差 |
| `0x0400` / `0x0401` | 编码器相关异常 | `0x0100` 编码器异常 |
| `0x0500` | 位置跟踪误差过大 | `0x1000` 位置误差 |

兼容码覆盖范围可能随项目协议或产品线调整，不建议只依赖 `0x603F` 做精确定位。
